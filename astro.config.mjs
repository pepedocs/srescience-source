// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://srescience.com',  // Replace with your GitHub Pages URL
  base: '/',                 // If deployed at username.github.io/repo/
});